services:
  - type: web
    name: jw-subtitle-extractor
    env: python
    buildCommand: pip install -r requirements.txt && playwright install chromium && playwright install-deps
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

4. **"Commit new file"** 클릭

---

## 📍 STEP 3: Render.com에 배포 (10분)

### 3-1. Render.com 가입
1. https://render.com 접속
2. **"Get Started"** 클릭
3. **"Sign up with GitHub"** 선택 (GitHub 계정으로 로그인)
4. Render가 GitHub에 접근 권한 요청하면 **"Authorize Render"** 클릭

### 3-2. 서버 배포하기
1. Render 대시보드에서 **"New +"** 버튼 클릭
2. **"Web Service"** 선택
3. 오른쪽에서 **"Connect a repository"** 섹션에서 `jw-subtitle-extractor` 찾아서 **"Connect"** 클릭
4. 설정 페이지에서:
   - **Name**: `jw-subtitle-extractor` (그대로 두기)
   - **Region**: `Oregon (US West)` 또는 가장 가까운 지역 선택
   - **Branch**: `main` (자동 선택됨)
   - **Runtime**: `Python 3` (자동 선택됨)
   - **Build Command**: 
```
     pip install -r requirements.txt && playwright install chromium && playwright install-deps
```
   - **Start Command**: 
```
     gunicorn app:app
